while True:
    h = int(input())
    if h == 0:
        break
    maior = 1
    while h > 1:
        if h > maior:
            maior = h
        if h % 2 == 0:
            h = int(h / 2)
        else:
            h = int(3 * h + 1)
    print(maior)


